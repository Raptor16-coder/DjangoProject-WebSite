### Web app description
Simple webapp

### TOOLS
All useful tools:

[ ] Run Web app : python3 app/manage.py runserver
[ ] Create directory : mkdir temp_folder

[ ] Change directory : cd ./CorrectFolder
[ ] Open current directory : explorer .
[ ] Open VisualStudio code in the currebt directory : code.

All git tools:
    [] Clone project                : git clone <Git-URL>
    [] Add all modifications        : git.add
    [] Attach a message             : git commit -m "MyMessage"
    [] Push modifications           : git push

    [] Add local credentials mail : git config user.email <email@gmail.org>
    [] Add local credentials username : git config user.name <GitUsername>
    
    [] CARE TO CREATE A OAUTH TOKEN :
        [] https://github.com/settings/tokens